{
  "name": "postcss-css-modules",
  "version": "0.0.0",
  "description": "PostCSS plugin to try & replicate the functionality of CSS modules",
  "main": "index.js",
  "scripts": {
    "test": "istanbul test --print both ./node_modules/mocha/bin/_mocha",
    "lint": "eslint --config=arenanet --env=mocha . && jscs --config jscs-preset-arenanet ."
  },
  "author": "Pat Cavit <npm@patcavit.com>",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^1.7.3",
    "eslint-config-arenanet": "^1.0.0",
    "istanbul": "^0.4.0",
    "jscs": "^2.5.0",
    "jscs-preset-arenanet": "^1.0.0",
    "mocha": "^2.3.3"
  },
  "dependencies": {
    "dependency-graph": "^0.4.1",
    "hasha": "^2.0.2",
    "lodash.uniq": "^3.2.2",
    "postcss": "^5.0.10",
    "postcss-selector-parser": "^1.3.0"
  }
}
